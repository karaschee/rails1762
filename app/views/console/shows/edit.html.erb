<h1>Console::Shows#edit</h1>

<%= form_for @show, url: console_show_path(@show), method: :patch do |f| %>
  <%= render partial: 'form', locals: { f: f } %>
  <p>
    <%= f.submit "Submit" %>
  </p>
<% end %>
