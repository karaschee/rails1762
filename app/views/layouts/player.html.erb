<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:main_title) ? yield(:main_title)+" | " : "" %>G-cores Fan Club</title>

  <!-- Stylesheets -->
  <%= stylesheet_link_tag "player", media: "all", "data-turbolinks-track" => true %>
  <%= render partial: "layouts/specific_css" %>
  <%= yield :stylesheets %>

  <%= csrf_meta_tags %>
</head>
<body>
  <%= render "layouts/player/header" %>  

  <section class="g_wrapper">
    <%= render partial: "layouts/player/side_nav" %>
    <%= yield %>
  </section>

  <footer class="">

  </footer>
  
  <!-- Javascripts -->
  <%= javascript_include_tag "player", "data-turbolinks-track" => true %>
  <%= yield :prepare_javascripts %>
  <%= render partial: "layouts/specific_js" %>
  <%= yield :javascripts %>
</body>
</html>